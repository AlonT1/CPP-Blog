/*
casting - the ability to convert between types
types are not enforced. they are used to "protect" the code

dynamic cast - does validation to ensure that the cast is valid.
evaluated at run time. used in inhertigance
*/
#include <iostream>

int main()
{
    std::cout << "Hello World!\n";
}
